# Build the plugin and publish dist/ to GitHub Pages.
#
# The bundle imports the Lolly engine from a SIBLING checkout, and copies the
# four community/filter-* tool directories in verbatim (see vite.config.ts), so
# the job clones the public lolly repo next to this one before building. GitHub
# Pages serves with `Access-Control-Allow-Origin: *`, which is what Penpot's
# plugin loader needs; users install from:
#   https://lolly-tools.github.io/lolly-plugins-penpot-filters/manifest.json
#
# One-time repo setting: Settings → Pages → Source: "GitHub Actions".
name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          path: lolly-plugins-penpot-filters

      # Sibling lolly checkout — SOURCE FILES ONLY. The bundle needs the engine
      # .ts sources plus community/filter-*/; the engine's two runtime deps
      # (handlebars, ajv) are dependencies of THIS repo and vite-aliased to its
      # node_modules, so lolly is never npm-installed here. No submodules
      # either: this plugin touches nothing under shells/.
      - uses: actions/checkout@v4
        with:
          repository: lolly-tools/lolly
          path: lolly

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install plugin deps
        working-directory: lolly-plugins-penpot-filters
        run: npm install

      - name: Typecheck
        working-directory: lolly-plugins-penpot-filters
        run: npm run typecheck

      - name: Build
        working-directory: lolly-plugins-penpot-filters
        run: LOLLY_DIR="$GITHUB_WORKSPACE/lolly" npm run build

      # Mounts all four filters through the real loader + runtime against the
      # just-built dist/tools/. Catches a tool renaming an input, or an engine
      # bump the manifests' engineVersion range no longer satisfies — both of
      # which would otherwise ship green and break in the panel.
      - name: Smoke test
        working-directory: lolly-plugins-penpot-filters
        run: LOLLY_DIR="$GITHUB_WORKSPACE/lolly" ./scripts/smoke.sh

      - uses: actions/upload-pages-artifact@v3
        with:
          path: lolly-plugins-penpot-filters/dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
