{
  "name": "lolly-penpot-filters",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Penpot plugin powered by the Lolly engine — posterize, halftone, scanline and Voronoi photo filters that output clean vector SVG straight onto the canvas, all client-side.",
  "license": "MPL-2.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && esbuild src/plugin.ts --bundle --format=iife --outfile=dist/plugin.js",
    "preview": "npm run build && vite preview --port 4403",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "smoke": "npm run build && ./scripts/smoke.sh"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "handlebars": "^4.7.8"
  },
  "devDependencies": {
    "@penpot/plugin-types": "^1.4.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.5.0",
    "vite": "^8.0.13"
  }
}
